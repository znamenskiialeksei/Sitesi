# УТВЕРЖДЕННЫЙ ПЛАН: ЛИКВИДАЦИЯ САМОПРОИЗВОЛЬНОГО СКРОЛЛА, ИСПРАВЛЕНИЕ ОШИБКИ ГОСТЕЙ NAN, СМАРТ-КОНТАКТЫ И CRM

## 1. Ликвидация самопроизвольного скролла [Чат, Кабинет гостя, Центр сообщений и Витрина]
- В `components/GuestCabinet/GuestChat.js`:
  - Замена глобального `bottomRef.current?.scrollIntoView` на изолированный скролл контейнера: `messagesContainerRef.current.scrollTop = messagesContainerRef.current.scrollHeight`.
  - Внедрение Scroll Lock Guard: автоскролл вниз выполняется ТОЛЬКО при первичной загрузке, отправке собственного сообщения или если пользователь находится у самого низа диалога [дистанция менее 120px].
  - Позиция скролла пользователя при периодическом 5-секундном поллинге сохраняется неизменной.
- В `components/HostCabinet/HostInbox.js`:
  - Внедрение аналогичного Scroll Lock Guard: скролл не сбивается при фоновом обновлении переписки.
- В `components/Modals/BusinessAssistantModal.js`:
  - Ликвидирован глобальный `scrollIntoView`, дергавший страницу. Заменен на изолированный скролл `chatContainerRef.current.scrollTop = chatContainerRef.current.scrollHeight`.

## 2. Исправление ошибки «Гостей: NaN» на карточках заявок хозяина
- В `pages/api/booking.js`:
  - В обработчике `master_get_chats` считываются и передаются поля `adults`, `children`, `total_guests`, `guests` из колонок 6, 7, 8 таблицы `BOOKINGS`.
- В `components/HostCabinet/HostReservations.js`:
  - Детерминированное вычисление `Number(req.guests) || (Number(req.adults || 0) + Number(req.children || 0)) || 1` с гарантированной защитой от `NaN`.

## 3. Кнопка быстрого перехода в чат на карточках заявок хозяина
- В `components/HostCabinet/HostReservations.js`:
  - На каждой карточке заявки рядом с кнопками одобрения добавлена кнопка `💬 Перейти в чат` с вызовом `onOpenChat(req)`.
- В `pages/host/index.js`:
  - Создан стейт `selectedChatSheet` и коллбэк `handleOpenChatFromReservation(req)`.
  - При клике происходит мгновенное переключение на вкладку `inbox` и выбор диалога гостя.
- В `components/HostCabinet/HostInbox.js`:
  - Подключен проп `initialSelectedSheet` с авто-выбором нужного диалога.

## 4. Умный список контактов в Центре сообщений с гостями [HostInbox.js]
- Смарт-бейджи на карточках контактов:
  - Статус регистрации: `👤 Зарегистрирован` / `👁️ Без рег.`
  - Уровень верификации: `✓ Email и Тел`, `✓ Email`, `✓ Тел`, `👤 Аккаунт`
  - Статус бронирования: `💳 Оплачено`, `⏰ HOLD 24ч`, `🎁 Спецпредложение`, `⏳ Запрос`
- Табы мультифильтрации над списком контактов:
  - `Все`, `С заявками`, `Оплаченные`, `В ожидании`, `Зарегистр.`, `Без регистр.`
- Сортировка:
  - По свежим сообщениям, срочности [HOLD/Запросы], сумме бронирования, имени гостя.

## 5. Полная поддержка турецких шаблонов сообщений [utils/templateResolver.js]
- Внедрен исчерпывающий маппинг всех турецких плейсхолдеров:
  `[ADINIZ]`, `[MISAFIR_ADI]`, `[ONAY_KODU]`, `[REZERVASYON_KODU]`, `[GİRİŞ_TARİHİ]`, `[ÇIKIŞ_TARİHİ]`, `[GİRİŞ_SAATI]`, `[ÇIKIŞ_SAATI]`, `[ADRES]`, `[HARİTA_URL]`, `[GİRİŞ YÖNTEMİ]`, `[WIFI_ADI]`, `[WIFI_ŞİFRESİ]`, `[KONUK_SAYISI]`, `[PLATFORM_ADI]`.
- Турецкие значения по умолчанию при `lang === 'tr'` [Misafir, 10 misafir, турецкие инструкции].

## 6. Сквозная верификация и предотвращение конфликтов данных
- В `pages/api/booking.js`:
  - Нормализовано создание имен листов диалогов в `getChatSheetName`: запрет разделителя `|`, исключение дублирования вкладок чатов для одного гостя.
  - Авторизация владельца строго ограничена категорией `МАСТЕР_ДОСТУП` листа `SETTINGS`, удален устаревший фоллбэк на лист `MASTER`.
  - Строгая проектная привязка ровно к двум таблицам Google Sheets:
    * Основная БД: `1ESfaH3FBOx-Z0Z1CKU8-c1cQZCE2YjJBiTvX0MV0A5Q`
    * Чаты: `1oiWwaT7KzbTdRS-pSCjHv-F84ymXlrmrkNE99IFD3rQ`
    * Корневая папка Drive: `11xBSWA02NypliPFbziRSMfC9aAPclYF_`

## 7. Фиксация шапки сайта и навигация по разделам витрины
- В `components/Navbar.js`:
  - Шапка зафиксирована `sticky top-0 z-50 bg-slate-900/90 backdrop-blur-md border-b border-white/10`.
  - Ссылки навигации обновлены на `/#about`, `/#amenities`, `/#reviews`, `/#gallery`, `/#catalog` для плавного перехода на разделы витрины из любых вложенных кабинетов `/guest` и `/host`.
  - Меню профиля оптимизировано под мобильные экраны: аватар гарантированно виден [shrink-0], меню ограничено шириной экрана `max-w-[calc(100vw-16px)]` и никогда не вылетает за границы вьюпорта.
