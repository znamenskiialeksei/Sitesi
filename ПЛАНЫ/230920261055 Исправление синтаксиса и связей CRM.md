# ПЛАН: Устранение синтаксической ошибки сборки HostReservations.js и проверка связей с CRM Google Таблиц

## 1. Метаданные плана
- Дата и время: 23.09.2026 10:55
- Статус: На рассмотрении пользователем
- Приоритет: Критический [блокирует Vercel build]
- Проект: villa-turaman-airbnb-platform

## 2. Суть проблемы
1. Ошибка сборки на Vercel в `components/HostCabinet/HostReservations.js`: на строке 295 после закрытия сетки `<div className="grid grid-cols-1 gap-4">` отсутствует закрывающий токен `)}` тернарного оператора, добавленного для проверки пустого списка `{displayedRequests.length === 0 ? (...) : (...)}`.
2. В `pages/api/booking.js` в обработчике `special_offer` формулы `=GOOGLETRANSLATE` содержали запятые вместо точек с запятой, что нарушало Правило 3.1.
3. В `google-apps-script/Code.js` в шаблоне настроек календаря требуется актуализировать параметры `paymentMode` и реквизиты IBAN.
