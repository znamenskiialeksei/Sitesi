# План: Устранение сбоя OpenSSL декодера приватного ключа + Защита офлайн кэша контента + 24 удобства виллы

- **Дата утверждения:** 18.09.2026 21:15
- **Статус:** ✅ Успешно реализовано
- **Ключевой модуль:** Безопасность & Отказоустойчивость / OpenSSL 3.0 + Content Fallbacks + Amenities Tri-Lingual

---

## 1. Цель и контекст задачи
1. Устранить фатальный сбой OpenSSL в Node.js 18+: `error:1E08010C:DECODER routines::unsupported` при инициализации `GoogleAuth` с некорректно форматированными или демонстрационными приватными ключами.
2. Исключить затирание файла `utils/content.json` пустыми объектами при сбое синхронизации.
3. Локализовать каталог 24 удобств (`components/Amenities.js`) на русский, английский и турецкий языки.

---

## 2. Затронутые файлы и компоненты
| Файл | Описание | Ссылка |
| :--- | :--- | :--- |
| `scripts/sync-content.js` | Валидация ключей и защита кэша от перезаписи | [scripts/sync-content.js](file:///c:/1%20Вилла%20Сайт%20—%20копия%20с%20Глобальными%20правилами%20проекта/villa-turaman-airbnb-platform/scripts/sync-content.js) |
| `scripts/init-google-sheets.js` | Функция нормализации `parsePrivateKey()` | [scripts/init-google-sheets.js](file:///c:/1%20Вилла%20Сайт%20—%20копия%20с%20Глобальными%20правилами%20проекта/villa-turaman-airbnb-platform/scripts/init-google-sheets.js) |
| `components/Amenities.js` | 24 удобства в 5 категориях на RU, EN, TR | [components/Amenities.js](file:///c:/1%20Вилла%20Сайт%20—%20копия%20с%20Глобальными%20правилами%20проекта/villa-turaman-airbnb-platform/components/Amenities.js) |

---

## 3. Ключевые фрагменты кода
```javascript
// Нормализатор RSA PEM приватных ключей Google Cloud
function parsePrivateKey(key) {
  if (!key) return '';
  let clean = key.trim();
  if ((clean.startsWith('"') && clean.endsWith('"')) || (clean.startsWith("'") && clean.endsWith("'"))) {
    clean = clean.slice(1, -1);
  }
  return clean.replace(/\\r\\n/g, '\n').replace(/\\n/g, '\n');
}
```

---

## 4. Результаты верификации
- Ошибка `DECODER routines::unsupported` ликвидирована.
- При отсутствии интернета или невалидном ключе сайт отдает полноценный контент из локального кэша `content.json`.
