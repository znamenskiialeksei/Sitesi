# План: 100% Структурная синхронизация 14 листов базы данных Google Sheets

- **Дата утверждения:** 18.09.2026 17:35
- **Статус:** ✅ Успешно реализовано
- **Ключевой модуль:** База данных / Google Sheets Schema Sync + `ensureSystemSheets()`

---

## 1. Цель и контекст задачи
Пользователь потребовал точного структурного соответствия рабочей базе данных `1ESfaH3FBOx-Z0Z1CKU8-c1cQZCE2YjJBiTvX0MV0A5Q`:
1. Устранить расхождения в именовании листа бронирований (`'Вилла'` с 11 колонками `!A:K` вместо ошибочного `'Бронирование'`).
2. Включить в инициализацию недостающие листы `GuestsAccess` и `ServiceOrders`.
3. Реализовать серверную функцию `ensureSystemSheets()`, автоматически создающую недостающие листы при любых API-запросах, применяющую темно-синий стиль шапки (`#263347`) и закрепляющую первую строку.
4. Добавить в API функции модератора для владельца: `special_offer`, `revoke_request`, `reject_request`, `master_get_lms`.

---

## 2. Затронутые файлы и компоненты
| Файл | Описание | Ссылка |
| :--- | :--- | :--- |
| `scripts/init-google-sheets.js` | Инициализация всех 14 листов со структурой и заголовками | [scripts/init-google-sheets.js](file:///c:/1%20Вилла%20Сайт%20—%20копия%20с%20Глобальными%20правилами%20проекта/villa-turaman-airbnb-platform/scripts/init-google-sheets.js) |
| `pages/api/booking.js` | CRM-бэкенд с функцией автосоздания структуры `ensureSystemSheets` | [pages/api/booking.js](file:///c:/1%20Вилла%20Сайт%20—%20копия%20с%20Глобальными%20правилами%20проекта/villa-turaman-airbnb-platform/pages/api/booking.js) |
| `pages/api/export-calendar.js` | Генерация iCal на основе 11 колонок листа `Вилла` | [pages/api/export-calendar.js](file:///c:/1%20Вилла%20Сайт%20—%20копия%20с%20Глобальными%20правилами%20проекта/villa-turaman-airbnb-platform/pages/api/export-calendar.js) |

---

## 3. Архитектурные решения
- Структура 14 листов приведена к эталону: `HomePage`, `MasterAccount`, `CalendarSettings`, `Вилла`, `Accounts`, `ExtraServices`, `VideoGuides`, `GuestsAccess`, `ServiceOrders`, `Gallery`, `About`, `Legal`, `Templates`, `Variables`.
- Инициализация дефолтного суперадмина (`MasterAccount`), плейсхолдеров (`Variables`) и шаблонов (`Templates`).
- Внедрение кэширования флага проверки таблицы в памяти (`is_sheets_formatted`), сокращающего расход лимитов Google Sheets API.

---

## 4. Результаты верификации
- Все 14 листов успешно создаются скриптом `init-google-sheets.js`.
- Заявки гостей корректно записываются в 11 колонок листа `Вилла`.
