# УТВЕРЖДЕННЫЙ ПЛАН : Устранение Синтаксической Ошибки в booking.js

## 1. Цель и контекст реализации
- Ликвидация сбоя компиляции Next.js : `Error: × Expected '}', got '<eof>' in pages/api/booking.js:1860:1`.
- Восстановление целостности синтаксической структуры и баланса фигурных скобок в обработчике `pages/api/booking.js`.

---

## 2. Архитектура решения
1. **Закрытие блока регистрации :**
   - Блок `if :action === 'register': { ... }` на строке 812 закрыт обязательной фигурной скобкой `}`.
2. **Ликвидация дублирования :**
   - Удален избыточный дублирующий блок `auto_register_guest` со строк 815-867.
   - Сохранен эталонный обработчик `action === 'auto_register_guest'` на строках 1192-1244, обеспечивающий регистрацию гостя с флагами верификации контактов в листе `ACCOUNTS`.
3. **Санитизация синтаксиса по правилу 2.7 :**
   - Удалены круглые скобки из служебных комментариев в кодовой базе модуля.

---

## 3. Статус утверждения
- План утвержден пользователем : Согласен.
- Режим выполнения : строго файловые инструменты без вызова терминала и модальных окон.
